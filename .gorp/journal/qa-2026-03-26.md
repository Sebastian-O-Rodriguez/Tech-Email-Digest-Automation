# QA Report — 2026-03-26

## Task: A5 — Tests for Article Fetcher
## Status: done

## Tests Written

`tests/test_article_fetcher.py` — 67 tests across 5 classes:

### TestFilterLinks (21 tests)
- test_keeps_valid_article_url — passes https article URL through unchanged
- test_keeps_http_url — passes http (non-https) article URL
- test_filters_mailto — drops mailto: scheme
- test_filters_anchor_only — drops bare #anchor hrefs
- test_filters_twitter_intent — drops twitter.com/intent share links
- test_filters_facebook_sharer — drops facebook.com/sharer links
- test_filters_linkedin_share — drops linkedin.com/shareArticle links
- test_filters_linkedin_share_variant — drops linkedin.com/share variant
- test_filters_github_domain — drops github.com URLs
- test_filters_youtube_domain — drops youtube.com URLs
- test_filters_reddit_domain — drops reddit.com URLs
- test_filters_unsubscribe_link — drops URLs containing "unsubscribe"
- test_filters_opt_out_link — drops URLs containing "opt-out"
- test_filters_tracking_click_redirect — drops click.*.* tracking URLs
- test_filters_list_manage — drops list-manage.com URLs
- test_filters_mailchimp — drops mailchimp.com URLs
- test_empty_input_returns_empty — empty list in, empty list out
- test_mixed_input_keeps_only_valid — validates filtering across mixed input
- test_non_http_scheme_filtered — drops ftp:// and other non-http(s) schemes
- test_www_prefix_stripped_for_domain_check — www.github.com matched to skip domain
- test_whitespace_stripped_from_url — leading/trailing whitespace handled

### TestDetectPaywall (11 tests)
- test_returns_true_for_402 — HTTP 402 status triggers paywall detection
- test_returns_true_for_403 — HTTP 403 status triggers paywall detection
- test_returns_true_for_subscribe_to_read — body phrase detection
- test_returns_true_case_insensitive — uppercased phrase still matches
- test_returns_true_for_members_only — "members only" phrase detection
- test_returns_true_for_sign_in_to_continue — "sign in to continue" detection
- test_returns_true_for_premium_content — "premium content" detection
- test_returns_true_for_paywall_keyword — "paywall" keyword detection
- test_returns_false_for_normal_article — clean article body is not a paywall
- test_returns_false_for_empty_body_with_200 — empty body + 200 is not a paywall
- test_returns_false_for_200_with_no_paywall_phrases — normal content returns False

### TestExtractArticleText (17 tests)
- test_extracts_title_from_title_tag — standard title extraction
- test_falls_back_to_h1_when_no_title_tag — h1 fallback when no title tag
- test_strips_site_name_suffix_pipe — "Article | TechCrunch" -> "Article"
- test_strips_site_name_suffix_dash — "Post - The Verge" -> "Post"
- test_strips_site_name_suffix_em_dash — "Feature — Wired" -> "Feature"
- test_strips_script_tags_from_text — script content excluded from body
- test_strips_style_tags_from_text — style rules excluded from body
- test_strips_nav_tags_from_text — nav links excluded from body
- test_strips_footer_tags_from_text — footer content excluded from body
- test_strips_aside_tags_from_text — aside content excluded
- test_strips_header_tags_from_text — header content excluded
- test_truncates_text_to_500_chars — text capped at 500 characters
- test_short_text_not_truncated — short text returned in full
- test_returns_empty_tuple_for_empty_string — ("", "") on empty input
- test_returns_empty_tuple_for_whitespace_only — ("", "") on whitespace-only
- test_empty_title_tag_falls_back_to_h1 — empty title tag treated as absent

### TestFetchArticles (13 tests)
- test_fetches_articles_from_email_links — happy path: returns ArticleContent list
- test_respects_10_article_cap — httpx.get called at most 10 times
- test_skips_paywalled_content — paywall response produces no ArticleContent
- test_skips_non_html_responses — non-text/html content-type skipped
- test_handles_httpx_timeout_gracefully — TimeoutException does not crash, returns []
- test_handles_connection_error_gracefully — ConnectError does not crash, returns []
- test_deduplicates_urls_across_emails — same URL across two emails fetched once
- test_returns_empty_list_when_no_emails_have_links — no links -> empty result
- test_returns_empty_list_for_empty_email_list — empty input -> empty result
- test_email_index_reflects_position_in_list — email_index is 1-based position
- test_skips_links_filtered_by_filter_links — filter_links integration; no HTTP calls made
- test_skips_empty_extraction — empty title+text -> article not appended
- test_handles_general_exception_gracefully — RuntimeError during get -> skipped

### TestFormatEmailsForLlm (6 tests)
- test_without_articles_format_unchanged — no Article: lines when articles=None
- test_with_articles_appended_after_links — Article: lines appear with article data
- test_article_line_appears_after_links_line — positional ordering validated
- test_multiple_articles_per_email_produce_multiple_lines — 2 articles -> 2 Article: lines
- test_articles_from_different_emails_routed_correctly — articles routed to correct email block
- test_none_articles_behaves_same_as_no_articles — None and omitted are equivalent

## Quality Gates

- [x] ruff check: pass (tests/test_article_fetcher.py and full src/ + tests/)
- [x] ruff format: pass (tests/test_article_fetcher.py; see issue below for models.py)
- [x] pytest: 121 passed, 0 failed (67 new + 54 pre-existing)
- [x] py_compile: pass (all .py files compile cleanly)

Note: `./scripts/quality-gate.sh all` shows 0/4 because the script calls bare `ruff` and
`python` which are not on PATH in this environment. All gates pass when called via `.venv/bin/`.

## Issues Found

- [low] `src/email_ingester/models.py` fails `ruff format --check`. The newly-added
  `ArticleContent` dataclass (added by backend agent in this sprint) has trailing
  whitespace in inline comments (`title: str        # From <title> or <h1>`).
  This is a pre-existing issue introduced with A4 backend work, not by these tests.
  Recommendation: backend agent should run `ruff format src/email_ingester/models.py`
  before the sprint closes.

## Blockers

None.
